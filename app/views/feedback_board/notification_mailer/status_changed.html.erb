<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #7C3AED; color: white; padding: 20px; border-radius: 6px 6px 0 0; }
    .content { background-color: #f9f9f9; padding: 20px; border-radius: 0 0 6px 6px; }
    .ticket-details { background-color: white; padding: 15px; border-radius: 6px; margin: 15px 0; }
    .status-change { background-color: #EDE9FE; padding: 15px; border-radius: 6px; margin: 15px 0; text-align: center; }
    .status-badge { display: inline-block; padding: 4px 8px; border-radius: 12px; font-size: 12px; font-weight: bold; margin: 0 5px; }
    .status-open { background-color: #10B981; color: white; }
    .status-planned { background-color: #3B82F6; color: white; }
    .status-in_progress { background-color: #F59E0B; color: white; }
    .status-complete { background-color: #6B7280; color: white; }
    .button { display: inline-block; background-color: #7C3AED; color: white; padding: 10px 20px; text-decoration: none; border-radius: 6px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📊 Status Updated</h1>
    </div>

    <div class="content">
      <p>The status of your feedback ticket has been updated!</p>

      <div class="status-change">
        <h3>Status Change</h3>
        <p>
          <span class="status-badge status-<%= @old_status %>"><%= @old_status&.humanize || 'Unknown' %></span>
          →
          <span class="status-badge status-<%= @new_status %>"><%= @new_status.humanize %></span>
        </p>
      </div>

      <div class="ticket-details">
        <h2><%= @ticket.title %></h2>
        <p><strong>Updated:</strong> <%= @ticket.updated_at.strftime("%B %d, %Y at %I:%M %p") %></p>
        <p><strong>Description:</strong></p>
        <p><%= simple_format(@ticket.description) %></p>
      </div>

      <p>
        <a href="<%= feedback_board_url(anchor: "ticket-#{@ticket.id}") %>" class="button">
          View Ticket
        </a>
      </p>

      <hr style="margin: 20px 0; border: none; border-top: 1px solid #ddd;">
      <p style="font-size: 12px; color: #666;">
        You're receiving this email because you submitted this feedback ticket.
      </p>
    </div>
  </div>
</body>
</html>
