<% if ticket_has_custom_field_values?(ticket, board) %>
  <div class="mt-6 border-t border-gray-200 pt-6">
    <h3 class="text-lg font-medium text-gray-900 mb-4">📋 Additional Information</h3>
    <div class="space-y-3">
      <% visible_custom_fields(ticket, board).each do |field| %>
        <div class="flex items-start">
          <dt class="text-sm font-medium text-gray-500 flex-shrink-0"><%= field.label %>:</dt>
          <dd class="text-sm text-gray-900 ml-4 flex-1">
            <%= format_custom_field_value(field, ticket.field_value(field)) %>
          </dd>
        </div>
      <% end %>
    </div>
  </div>
  <hr class="my-4" />
<% end %>
