<%= render 'alto/shared/filter_dropdown',
    field_name: :status,
    options: [["All statuses", ""]] + (@board.has_status_tracking? ? @statuses.map { |status| [status.name, status.slug] } : []),
    current_value: params[:status] %>
