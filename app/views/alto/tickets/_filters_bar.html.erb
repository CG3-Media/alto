<div class="flex justify-between items-center flex-wrap gap-4 w-full md:w-auto">
  <div class="flex flex-col md:flex-row md:items-center gap-4 w-full md:w-auto">
    <%= render 'view_toggle' if show_view_toggle? %>
    <%= render 'status_filters' if show_status_filters? %>
    <%= render 'tag_filters' if @available_tags.any? %>
  </div>

  <%= render 'sort_toggle' %>
</div>
