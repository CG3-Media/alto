<!DOCTYPE html>
<html>
  <head>
    <title>Feedback Board</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Use your app's Vite assets -->
    <%= vite_client_tag if defined?(vite_client_tag) %>
    <%= vite_javascript_tag 'application' if defined?(vite_javascript_tag) %>

    <!-- Tailwind CSS for FeedbackBoard styling -->
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <header class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center py-4">
          <div class="flex items-center">
            <%= link_to feedback_board.root_path, class: "text-xl font-semibold text-gray-900" do %>
              🗣️ Feedback Board
            <% end %>
          </div>
          <nav class="flex space-x-4">
            <%= link_to "All Tickets", feedback_board.tickets_path,
                class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
            <% if can_submit_tickets? %>
              <%= link_to "New Ticket", feedback_board.new_ticket_path,
                  class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <% if notice %>
        <div class="mb-4 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-md">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-md">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
