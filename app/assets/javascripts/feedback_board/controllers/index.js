// Import and register all Stimulus controllers for FeedbackBoard
// This will be loaded by the host application's Stimulus setup

import UpvoteController from "./upvote_controller"

// Export for manual registration by host app if needed
export { UpvoteController }
